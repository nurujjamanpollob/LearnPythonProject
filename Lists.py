# Python basic list of String example
basicList = ["Hi", "this is", "python", "language"]

# Print the basicList
# Use str(object) method to convert object to String
print("The basic list: " + str(basicList))

# Create a list of mixed data types in a single list
mixedList = ["Hi", "I am number", 10, "in my college"]

# Print the mixedList
# Use str(object) method to convert object to String
print("The mixed list: " + str(mixedList))

# Create a nested list
nestedList = ["Hi", "I gonna create a", "nested list now -> ", ["I", "am", "nested", "list", "😁"]]

# Print the mixedList
# Use str(object) method to convert object to String
print("The nested list: " + str(nestedList))
